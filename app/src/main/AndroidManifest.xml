<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pranshu.portfolioshare">

    <!-- NFC permissions -->
    <uses-permission android:name="android.permission.NFC" />

    <!--
      Declare that this app requires NFC.
      This is good, but for HCE we also need the nfc.hce feature.
    -->
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <!--
      FIX #1: Added the HCE feature requirement.
      This tells the system and the Play Store that HCE is mandatory for this app.
    -->
    <uses-feature android:name="android.hardware.nfc.hce" android:required="true" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PortfolioShare"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PortfolioShare">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          FIX #2: Changed android:name to ".HceService"
          This now correctly points to your "class HceService"
          (The file name "MyHostApduService.kt" doesn't matter, only the class name)
        -->
        <service
            android:name=".HceService"
            android:permission="android.permission.BIND_NFC_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

    </application>
</manifest>